Bootstrap: docker
From: pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime

%files
    $PWD/apptainer-req.txt requirements.txt

%post
    apt-get update && \
    apt-get install -y libgl1 libglib2.0-0 && \
    pip install --root-user-action=ignore -r requirements.txt
